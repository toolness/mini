function setupLevel() {
  // Somebody messed up the code and the platforms are not properly aligned
  // Change the numbers so you can reach the goal

  // Black platforms are fragile places to try and stand on.
  // Change the color on the platforms
  createPlatform(40, 100, 80, 10, 'green');
  createPlatform(160, 200, 80, 10, 'black');
  createPlatform(160, 300, 80, 10, 'Black');
  createPlatform(70, 370, 80, 10, 'green');
  createPlatform(510, 400, 80, 10, 'black');
}
